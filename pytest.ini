[pytest]
testpaths = tests
# Put the repo root on sys.path so tests import the package without an install
# step. Without this, pytest 8 on Linux cannot find it and CI fails while a
# local run passes, because the shell's cwd was doing the work.
pythonpath = .
